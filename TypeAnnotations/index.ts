// Declare a variable for favorite movie name
let favoriteMovie: string = "How to Train Your Dragon";

// Create a template string to introduce yourself
const name: string = "John";
const age: number = 30;
const intro: string = `Hello, my name is ${name} and I am ${age} years old.`;
console.log(intro);

// Initialize a constant for current mood
const isHappy: boolean = true;

// Combine a user's first and last name using template literals
const firstName: string = "Eric";
const lastName: string = "Doe";
const fullName: string = `${firstName} ${lastName}`;
console.log(fullName);

// Variable for user's age with type annotation
let userAge: number;
userAge = 25;

// Template string to display a product name and its price
const productName: string = "Smartphone";
const price: number = 105000;
const productDisplay: string = `Product: ${productName}, Price: $${price}`;
console.log(productDisplay);

// Variable to store the number of pets with number type annotation
let numberOfPets: number = 2;

// Variable with boolean type annotation for birthday
let isBirthdayToday: boolean = false;

// Template string with user's information
const userDetails: string = `Name: ${firstName} ${lastName}, Age: ${userAge}`;
console.log(userDetails);

// Constant to hold birth year
const birthYear: number = 1990;

// Template string to combine city and country
const city: string = "New York";
const country: string = "USA";
const address: string = `${city}, ${country}`;
console.log(address);

// Variable for today's temperature
let todayTemperature: number = 25; 
console.log(`${todayTemperature} Celsius`);
export{}